<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.IPersonDAO">
    <insert id="insertPerson" parameterType="Person">
        INSERT INTO PERSON(ID,USERNAME,AGE,MOBILEPHONE)VALUES (#{id},#{userName},#{age},#{mobilePhone})
    </insert>

    <select id="queryById" parameterType="int" resultType="Person">
        SELECT * FROM PERSON WHERE ID=#{id}
    </select>

    <update id="updatePerson" >
        update person set UserName=#{username},age=#{age},mobilePhone=#{mobilePhone} where id=#{id}
    </update>

    <select id="selectAll" resultType="Person">
         select * from person
   </select>

    <delete id="delPerson">
        DELETE  person WHERE  id=#{ID}
    </delete>

    <select id="searchPerson" parameterType="Person" resultType="Person">
           SELECT * FROM  person where USERNAME LIKE '%' #{USERNAME} '%'
    </select>

    <select id="findPersonByCondition" resultType="String">

          SELECT * FROM  person where USERNAME LIKE concat('%',#{ss},'%')
    </select>

</mapper>

