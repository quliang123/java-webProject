<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.IUserInfoDAO">
    <select id="isLogin" parameterType="String" resultType="boolean">
           select COUNT(1) from UserInfo where UserName=#{0} and UserPwd=#{1}
   </select>

    <select id="getAllUser" resultType="user">
            select * from UserInfo
   </select>

    <select id="getUserByCondition" resultType="user">
            select * from UserInfo where UserName=#{0} and UserPwd=#{1}
    </select>

    <update id="modifyUserIp" parameterType="user">
            update userinfo set ip=#{Ip},lastLoginTime=#{lastLoginTime} where username =#{UserName} and userpwd=#{UserPwd}
    </update>
</mapper>